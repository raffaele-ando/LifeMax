node_modules
node_modules/
# l'isola React esce da Vite in assets/react/ e da lì la prende costruisci.mjs,
# che ne mette una copia con l'impronta nel nome dentro assets/pacco/. Quella
# serve, questa no: è un passaggio intermedio.
assets/react/

# L'USCITA DELLA RISCRITTURA, IGNORATA FINCHÉ È UN CANTIERE.
# Attenzione alla fine: qui GitHub Pages serve il ramo così com'è, senza
# nessun passaggio di build — per questo `assets/pacco/` è COMMITTATO. Quando
# la riscrittura passa le prove e Pages punterà a /docs, questa riga va
# tolta, se no il sito nuovo non esiste per nessuno.
docs/
