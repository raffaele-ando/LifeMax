{
  "name": "lifemax",
  "private": true,
  "comment-type": "NIENTE `type: module`: le trenta prove in prove/ sono CommonJS e usano require(). La configurazione di Vite risolve la sua metà da sé chiamandosi vite.config.mts.",
  "description": "TypeScript + React + Vite. `npm run build:nuovo` controlla i tipi e scrive il sito in docs/, che è quello che GitHub Pages serve — il ramo così com'è, senza nessun passaggio di build. Per questo docs/ è committato, e prove/pacco.js controlla che non sia più vecchio dei sorgenti.",
  "scripts": {
    "dev": "vite",
    "tipi": "tsc --noEmit",
    "build:nuovo": "tsc --noEmit && vite build",
    "prove": "node prove/pacco.js && node prove/pezzi.js && node prove/segni.js && node prove/intestazioni.js"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@vitejs/plugin-react": "^6.1.1",
    "esbuild": "^0.28.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
